1. don t show the same text for adaptive and control sessions

2. change nasa rank to 1-10 and update to not have sliders, just click to select a number. This is more intuitive and less error-prone than the current slider system. It also allows us to remove the "error" state where users forget to move the slider from the default position.

3. make sure that this will work on phone browsers as well, since some users may want to participate using a mobile device. This means ensuring that the test buttons are large enough to tap and that the layout is responsive. and the metrics collection works on mobile as well.

4. in /dashboard add filtering by age, disability, computer expertise.

5. dont use "system" in SUS and NASA-TLX instructions, since that may be confusing to users who dont know what "system" means in this context. Instead, say "the interface you just used" or something more user-friendly.

6. Questions to be in natural language and easy to understand, especially for users with low computer expertise. Avoid jargon and technical terms. For example, instead of "mental demand" say "How mentally exhausting was the task?" and instead of "effort" say "How hard did you have to work to complete the task?"
